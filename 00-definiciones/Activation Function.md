Una función matemática (usualmente no lineal) aplicada a la salida de una neurona. Decide si la neurona debe "dispararse" (activarse) o no, introduciendo la no-linealidad necesaria para aprender patrones complejos.

- **Ejemplo (ReLU):** Si el cálculo de una neurona da un valor negativo ($-5$), la función **ReLU** lo convierte en $0$ (neurona apagada). Si es positivo ($5$), lo deja pasar tal cual.