El conjunto total de variables internas que el modelo ajusta durante el entrenamiento. Se compone principalmente de **Weights** ($w$) y **Biases** ($b$).

- **Ejemplo:** Un modelo GPT tiene miles de millones de **Parameters**. Son los "diales" internos que se giran milimétricamente para que el modelo genere texto coherente.